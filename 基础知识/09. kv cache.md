## kv cache
> 我的理解： 在infer的时候，自回归，新token作为q和之前的token的k算attention map，然后用他们和自己的的value作为加权求和得到自己的新value。每次新token前面的k，v都一样，所以不需要重复计算，我们可以保存下来重复利用。

### 1. 显存计算
Size=2 × Batch × Layers × Heads × Seq_Len × Head_Dim × Precision。FP16 是 2 Bytes，FP32 是 4 Bytes。

### 2. MHA/MQA/GQA
每个head不同q，k，v -> 每个head共享k，v -> 折中方案。把 Head 分组（比如 8 组），每组共享 K,V.效果接近 MHA，
